# DAY6–7 行程调整 + 门票标注 + 地图箭头（设计）

> 日期：2026-07-23  
> 状态：**已落地**（coords + seed + generate + VPS；云端以 reset 后为准）  
> 范围：`coords.json` / seed / `generate_html.py` 地图折线；云端需「恢复初始」或等价写入才覆盖线上文档  
> **行程内容权威**：以当前 `coords.json` 为准。后经用户优化 JSON，大黑山岛/蓬莱阁已调整到 **DAY5**，养马岛与市区海滨在 **DAY6**；下文「行程」小节仅保留决策当时快照，勿当现役时刻表。

## 行程

### DAY6

按用户截图保留四点：

| 时间 | 点 | 说明 |
|------|-----|------|
| 09:00 | 养马岛 | 上午 |
| 13:30 | 蓬莱阁 | 下午 |
| 15:45 | 八仙过海 | 与蓬莱阁同区 |
| 17:30 | 大黑山岛 | 长岛；描述注明船班以当日为准 |

酒店出发/回宿点保留。

### DAY7

- 退房后上午：**芝罘南山公园**、**西炮台国防公园**（避开前几天已去点）  
- 下午回威海：韩乐坊 / 还车 / 站旁住宿，时间顺延  

## 门票

- 写在 `desc` 末尾：`门票：免费` 或 `门票约¥xx（以现场为准）`  
- 优先 **scenic / culture**；船票等交通费可写在同条描述  
- 约价来自公开资料，不保证与现场一致  

## 地图箭头

- 仅地图折线加方向箭头（百度 `IconSequence` / Symbol）  
- 驾车路线与直线备用均显示；时间轴不加卡片间 ↓  

## 同步

1. 改 `coords.json`  
2. `scripts/export_seed.py` + `generate_html.py`  
3. `deploy_vps.py`  
4. 线上共享文档：恢复初始或手工对齐（deploy 不自动覆盖 `itinerary.json`）
